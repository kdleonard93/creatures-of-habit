[package]
name = "creatures-of-habit"
version = "0.1.0"
description = "A gamified habit tracking application that helps users build consistent routines"
authors = ["kdleonard93"]
license = "UNLICENSED"
repository = "https://github.com/kdleonard93/creatures-of-habit"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2", features = [] }
tauri-plugin-log = "2"
tauri-plugin-opener = "2"
tauri-plugin-window-state = "2"

[profile.release]
codegen-units = 1
lto = true
strip = true
